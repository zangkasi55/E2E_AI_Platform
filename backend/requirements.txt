# Agentic AI Platform PoC — backend runtime dependencies (Python 3.11)
# Pinned loosely for PoC; tighten before production.

fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
pydantic-settings==2.3.4
httpx==0.27.0
python-dotenv==1.0.1

# Agent orchestration (Semantic Kernel pattern)
semantic-kernel==1.3.0

# Azure OpenAI / model serving
openai==1.35.7

# Azure SDKs (identity, state store, observability)
# NOTE: azure-monitor-opentelemetry and opentelemetry-api/sdk MUST stay version
# aligned. The previous mix (1.6.0 + otel 1.25.0) was incompatible, so
# configure_azure_monitor() raised at startup and telemetry silently no-op'd
# (empty Foundry Traces/Monitor). These versions are verified to configure +
# export gen_ai spans successfully.
azure-identity==1.19.0
azure-cosmos==4.7.0
azure-monitor-opentelemetry==1.8.8
opentelemetry-api==1.40.0
opentelemetry-sdk==1.40.0

# Eventing for HITL (Service Bus) — optional in mock mode
azure-servicebus==7.12.2

# Microsoft Purview governance / sensitivity-label resolution (live mode only;
# lazily imported by app.governance.sensitivity — not required in mock mode)
azure-purview-datamap==1.0.0b1

# Dev / test
pytest==8.2.2
